name: ASSETS UI CD - OCP

on:
  push:
    branches:
      - main
    paths:
      - "bor-ui/**"
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment"
        required: true
        type: choice
        options:
        - dev
        - test
        - sandbox
        - prod
      tagname:
        description: "Specify a previous version (git tag) to deploy"
        required: false
        default: ""

jobs:
  bor-ui-cd:
    uses: bcgov/bcregistry-sre/.github/workflows/ui-cd-private.yaml@main
    with:
      environment: ${{ inputs.environment }}
      tagname: ${{ inputs.tagname }}
      working_directory: "./bor-ui"
    secrets:
      APP_NAME: "bor-ui"
      OP_CONNECT_URL: ${{ secrets.OP_CONNECT_URL }}
      OP_CONNECT_TOKEN: ${{ secrets.OP_CONNECT_TOKEN }}
