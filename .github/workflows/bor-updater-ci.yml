name: BOR Solr Updater CI

on:
  pull_request:
    branches:
      - main
      - feature*
      - hotfix*
    paths:
      - "queue-services/bor-solr-updater/**"
  workflow_dispatch:

defaults:
  run:
    shell: bash
    working-directory: ./queue-services/bor-solr-updater

jobs:
  bor-updater-ci:
    uses: bcgov/bcregistry-sre/.github/workflows/backend-ci.yaml@main
    with:
      app_name: "vor-solr-updater"
      working_directory: "./queue-services/bor-solr-updater"
      codecov_flag: "bor-solr-updater"

