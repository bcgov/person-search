apiVersion: template.openshift.io/v1
kind: Template
metadata:
  name: preload-job-template
  labels:
    app: bor-solr
    name: bor-solr-preload-${ENV}
    role: job
objects:
  - apiVersion: batch/v1
    kind: Job
    metadata:
      name: bor-solr-preload
      namespace: 1dfe78-${ENV}
      labels:
        app: bor-solr
        name: bor-solr-preload-${ENV}
        role: job
    spec:
      template:
        metadata:
          name: bor-solr-preload-${ENV}
          labels:
            app: bor-solr
            name: bor-solr-preload-${ENV}
            role: job
        spec:
          containers:
            - command:
                - /bin/sh
                - -c
                - cd /opt/app-root; ./run.sh
              env:
                - name: ORACLE_USER
                  valueFrom:
                    secretKeyRef:
                      key: ORACLE_USER
                      name: bor-solr-importer-${ENV}-secret
                - name: ORACLE_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      key: ORACLE_PASSWORD
                      name: bor-solr-importer-${ENV}-secret
                - name: ORACLE_DB_NAME
                  valueFrom:
                    secretKeyRef:
                      key: ORACLE_DB_NAME
                      name: bor-solr-importer-${ENV}-secret
                - name: ORACLE_HOST
                  valueFrom:
                    secretKeyRef:
                      key: ORACLE_HOST
                      name: bor-solr-importer-${ENV}-secret
                - name: ORACLE_PORT
                  valueFrom:
                    secretKeyRef:
                      key: ORACLE_PORT
                      name: bor-solr-importer-${ENV}-secret
                - name: DATABASE_HOST_LEAR
                  valueFrom:
                    secretKeyRef:
                      name: bor-solr-importer-dev-secret
                      key: DATABASE_HOST_LEAR
                - name: DATABASE_NAME
                  valueFrom:
                    secretKeyRef:
                      key: DATABASE_NAME
                      name: bor-solr-importer-${ENV}-secret
                - name: DATABASE_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      key: DATABASE_PASSWORD
                      name: bor-solr-importer-${ENV}-secret
                - name: DATABASE_PORT
                  valueFrom:
                    secretKeyRef:
                      key: DATABASE_PORT
                      name: bor-solr-importer-${ENV}-secret
                - name: DATABASE_USERNAME
                  valueFrom:
                    secretKeyRef:
                      key: DATABASE_USERNAME
                      name: bor-solr-importer-${ENV}-secret
                - name: POD_NAMESPACE
                  valueFrom:
                    secretKeyRef:
                      key: POD_NAMESPACE
                      name: bor-solr-importer-${ENV}-secret
                - name: SOLR_SVC_LEADER_URL
                  valueFrom:
                    secretKeyRef:
                      key: SOLR_SVC_PRELOAD_URL
                      name: bor-solr-importer-${ENV}-secret
                - name: SOLR_BATCH_UPDATE_SIZE
                  valueFrom:
                    secretKeyRef:
                      key: SOLR_BATCH_UPDATE_SIZE
                      name: bor-solr-importer-${ENV}-secret
                - name: REINDEX_CORE
                  value: "True"
                - name: PRELOADER_JOB
                  value: "True"
                - name: FLASK_ENV
                  valueFrom:
                    secretKeyRef:
                      key: FLASK_ENV
                      name: bor-solr-importer-${ENV}-secret
              image: image-registry.openshift-image-registry.svc:5000/1dfe78-tools/bor-solr-importer:${ENV}
              imagePullPolicy: Always
              name: bor-solr-preload-${ENV}
              resources:
                limits:
                  cpu: '1'
                  memory: 16Gi
                requests:
                  cpu: 900m
                  memory: 12Gi
              terminationMessagePath: /dev/termination-log
              terminationMessagePolicy: File
          activeDeadlineSeconds: 12000
          dnsPolicy: ClusterFirst
          restartPolicy: Never
          schedulerName: default-scheduler
          securityContext: {}
          terminationGracePeriodSeconds: 300
parameters:
  - description: Environment job will be created in
    name: ENV
    required: true
